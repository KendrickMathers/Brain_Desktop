import QtQuick
import "../theme"

Rectangle {

    width: parent.width

    height: 1

    color: Qt.rgba(
        Theme.border.r,
        Theme.border.g,
        Theme.border.b,
        0.6
    )
}Theme.border
